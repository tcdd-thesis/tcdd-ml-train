# Environment configuration for Sign Detection System

# Backend Node.js server
PORT=5000
NODE_ENV=production

# Python camera server URL (change if running on different machine)
PYTHON_SERVER=http://localhost:5001

# Frontend API URL (for React app)
REACT_APP_API_URL=http://localhost:5000

# Camera settings (for Python server)
CAMERA_WIDTH=640
CAMERA_HEIGHT=480
CAMERA_FPS=30

# Detection settings
CONFIDENCE_THRESHOLD=0.5
DETECTION_INTERVAL=1
JPEG_QUALITY=80

# Model path (relative to backend/python/)
MODEL_PATH=model/best.pt

# Performance tuning
# Set to 1 for high performance Pi 5, 2-3 for lower end
# This controls how many frames to skip between detections
# DETECTION_INTERVAL=1  # Run detection every frame (high CPU)
# DETECTION_INTERVAL=2  # Run detection every 2nd frame (balanced)
# DETECTION_INTERVAL=3  # Run detection every 3rd frame (low CPU)

# For production on Raspberry Pi, use:
# REACT_APP_API_URL=http://raspberrypi.local:5000
# Or use the Pi's IP address:
# REACT_APP_API_URL=http://192.168.1.100:5000

# Proxy timeout (milliseconds)
PROXY_TIMEOUT=30000

# CORS origin (for production, set specific origins)
# CORS_ORIGIN=http://raspberrypi.local:3000
